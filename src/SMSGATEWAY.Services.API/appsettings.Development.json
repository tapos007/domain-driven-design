{
  
  // dotnet ef migrations add "Initial" -p "SMSGATEWAY.Infra.Identity" -c  AuthDbContext -s "SMSGATEWAY.Services.API"  
  // dotnet ef database update -p "SMSGATEWAY.Infra.Identity" -c  AuthDbContext -s "SMSGATEWAY.Services.API" 
  // dotnet ef migrations add "Initial" -p "SMSGATEWAY.Infra.Data" -c  ApplicationDbContext -s "SMSGATEWAY.Services.API"
  // dotnet ef database update -p "SMSGATEWAY.Infra.Data" -c  ApplicationDbContext -s "SMSGATEWAY.Services.API"
  // dotnet ef migrations add "Initial" -p "SMSGATEWAY.Infra.Data" -c  EventStoreSqlContext -s "SMSGATEWAY.Services.API" -o "Migrations/EventStoreSQL" 
  // dotnet ef database update -p "SMSGATEWAY.Infra.Data" -c  EventStoreSqlContext -s "SMSGATEWAY.Services.API"
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "ConnectionStrings": {
    "DefaultConnection": "server=localhost;user=root;password=Rcis123$..;database=DDDTest"
  }
}
